name: Promote latest to prod
on:
  workflow_dispatch:

env:
  IMAGE_NAME: "${{ github.event.repository.name }}"
  IMAGE_REGISTRY: "${{ secrets.IMAGE_REGISTRY }}"
  TAG_OLD: "latest"
  TAG_NEW: "prod"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: false

jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - run: |
          CONTENT_TYPE="application/vnd.docker.distribution.manifest.v2+json"
          U="${{ secrets.REGISTRY_USER }}:${{ secrets.REGISTRY_PASSWORD }}"
          MANIFEST=$(curl -v -H "Accept: ${CONTENT_TYPE}" -u "${U}" --no-progress-meter "https://${IMAGE_REGISTRY}/v2/${IMAGE_NAME}/manifests/${TAG_OLD}")
          curl -v -X PUT -H "Content-Type: ${CONTENT_TYPE}" -d "${MANIFEST}" -u "${U}" --no-progress-meter "https://${IMAGE_REGISTRY}/v2/${IMAGE_NAME}/manifests/${TAG_NEW}"
